{
  "name": "merchant-sdk-minimal-example",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap:local": "node --env-file=.env bootstrap-local-merchant.mjs",
    "start": "node --env-file=.env.local server.mjs",
    "dev": "node --watch --env-file=.env.local server.mjs",
    "check": "node --check bootstrap-local-merchant.mjs && node --check server.mjs"
  },
  "dependencies": {
    "@railbridgeai/merchant-sdk": "^0.1.0",
    "express": "^5.1.0"
  }
}
